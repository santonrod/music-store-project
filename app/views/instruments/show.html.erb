<p id="notice"><%= notice %></p>

<p>
  <strong>Category:</strong>
  <%= @instrument.category %>
</p>

<p>
  <strong>Brand:</strong>
  <%= @instrument.brand %>
</p>

<p>
  <strong>Year:</strong>
  <%= @instrument.year %>
</p>

<p>
  <strong>Details:</strong>
  <%= @instrument.details %>
</p>

<p>
  <strong>Price:</strong>
  <%= @instrument.price %>
</p>

<% if @instrument.user_id == current_user.id %>
  <td><%= link_to 'Edit', edit_instrument_path(@instrument) %></td>
  <td><%= link_to 'Destroy', @instrument, method: :delete, data: { confirm: 'Are you sure?' } %></td>
<% else %>
<% end %> 

<%= link_to 'Back', user_instruments_path(@instrument) %>

<%= link_to 'Owners Details', user_path(@instrument.user_id)  %>

<%= link_to 'Purchase', new_instrument_appointment_path(@instrument)  %>

